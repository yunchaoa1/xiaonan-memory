# OPC 精简候选 Skill 验收报告

## 验收范围与判定口径

本报告验收 `D:\Hermes\xiaonan-memory\opc-lean-plan\candidates\` 下的 6 份候选稿，不替换原 Skill，不修改任何候选稿。逐稿检查四项：

1. 是否保留 `00_保留借鉴删除清单.md` 中已验证成果。
2. 是否删除平台工程，且没有误删必要门禁。
3. 是否把职责、输入、唯一输出和阻断条件写清楚。
4. 是否存在未经给定证据支持的 `PASS`，或夸大的市场/产品化方案。

本次事实基线仅采用任务给定资料和两份索引：FIX-FIRST-DELIVERY PASS（`1/0/0/1`，六门禁 `[4,4,4,4,3,4]`）、多样性/blocked/来源隔离 PASS、小说改剧本 `38/44` 发布线、资产 `v0.11` 独立 PASS 且问题 0、OTIO 三镜真实 PASS、GPT Image 横屏原生 `3840x2160` 真实 PASS；H3 真实视频为 `NOT_RUN`。静态结构 PASS 不等同于媒体或视频 PASS。

判定说明：`PASS` 表示四项均无必须修改的问题；`REVISE` 表示存在会造成验收误读、证据越界或契约歧义的最小必要修订。行号均为本次读取时的候选稿行号。

## 文件清单与读取行数

| 候选稿 | 读取行数 |
|---|---:|
| `writing-opc-entry/SKILL.md` | 180 |
| `novel-to-screenplay/SKILL.md` | 119 |
| `screenplay-asset-extraction/SKILL.md` | 156 |
| `screenplay-to-15s-storyboard/SKILL.md` | 164 |
| `gpt-image-subject-assets/SKILL.md` | 141 |
| `minimax-h3-shot-prompt/SKILL.md` | 141 |

## 逐文件验收

### 1. `writing-opc-entry/SKILL.md`：PASS

- **已验证成果：通过。** 保留一次提交、客户过程中不参与、唯一首交、下游封闭、六门禁、`completed`/`blocked` 互斥、多样性与来源隔离成果（第 16-35、51-57、97-117、167-180 行）。固定回归明确记录 `FIX-FIRST-DELIVERY` `1/0/0/1`、六门禁 `[4,4,4,4,3,4]`、多样性 A/B 和 blocked/来源隔离 PASS，和给定证据一致（第 167-180 行）。
- **平台工程与必要门禁：通过。** 明确不负责平台工程（第 16-18、27 行），且保留六项逻辑门禁、来源隔离、阻断互斥和下游封闭门禁；没有发现误删必要门禁。
- **职责/输入/唯一输出/阻断：通过。** 职责边界、最小 `customer_idea` 输入、唯一小说或唯一阻断对象均明确；完成分支不得同时有 `novel` 与 `blockage`，阻断分支不得携带正文（第 29-49、76-81、119-145 行）。
- **证据与市场夸大：通过。** 明确不得预填或臆称 PASS，也没有把商业偏好当逻辑验收；固定证据没有外推成通用市场成功（第 116-117、167-180 行）。
- **最小修改清单：** 无。

### 2. `novel-to-screenplay/SKILL.md`：PASS

- **已验证成果：通过。** 保留拟态还原、信息层、心理转行动、因果与代价、场景功能、人物/状态/来源追溯，并明确不越界到镜头、提示词和模型参数（第 14-22、41-57、71-84 行）。固定回归保留 `38/44` 发布线及具体可追溯事实（第 90-97 行）。
- **平台工程与必要门禁：通过。** 明确删除 runner、数据库、API 控制器、checkpoint 和评分服务等平台工程（第 85-89 行）；十项门禁仍覆盖来源、事实、能动性、信息、场景、表演、连续性、追溯和发布线（第 37-57 行）。
- **职责/输入/唯一输出/阻断：通过。** 一个可识别小说/章节源文本作为唯一输入；仅输出一个完整剧本包、`INPUT_ERROR` 或 `ADAPTATION_FAIL`，不会输出部分成功或多个草稿（第 23-35 行）。
- **证据与市场夸大：通过。** `38/44` 被写成已知发布线，没有外推为所有题材定稿；未将可选方法/库写成平台或市场方案（第 85-97 行）。
- **最小修改清单：** 无。

### 3. `screenplay-asset-extraction/SKILL.md`：PASS

- **已验证成果：通过。** 保留 identity master/story state 分离、字符快照不内嵌外部资产、道具/能力有向状态链、镜头变化的 start/end 原子绑定、空间核心方向和 `v0.11` 独立 PASS/问题 0（第 40-69、91-105、124-134 行）。
- **平台工程与必要门禁：通过。** 明确不建设 DVC、数据库表、资产 registry、通用 runner 或平台（第 119-123 行）；十项门禁保留实体证据、状态链、绑定、依赖和 QC，且保留“未运行媒体不得 PASS”（第 50-71 行）。
- **职责/输入/唯一输出/阻断：通过。** 单一已批准剧本包输入，职责限定为提取和绑定；输出为一个完整结构化资产包或一个明确阻断/失败结果，部分数据、候选和提示词不得外显（第 19-38、113-118 行）。
- **证据与市场夸大：通过。** `v0.11` 的独立 PASS/0 问题与给定基线一致；ScriptBreak/OSF 被限定为可选分类参考，没有声称其集成或市场能力（第 119-129 行）。
- **最小修改清单：** 无。

### 4. `screenplay-to-15s-storyboard/SKILL.md`：PASS

- **已验证成果：通过。** 保留单镜不超过 15 秒、核心不超过 14 秒、一空间/一目标/一核心动作/一主运镜、情绪导演链、可表演动作、声音功能、首中尾状态、剪辑点、动态宫格、无文字调度图、主体自身方向及连续性接口（第 37-48、60-110 行）。OTIO 被限制为交换和帧率数学，未扩展为平台（第 72-78、106-112 行）。
- **平台工程与必要门禁：通过。** 明确不建设平台、数据库、runner，不实现 Kitsu 集成或任务状态系统（第 17-25、106-112 行）；十项硬门禁保留时长、状态、情绪、声音、空间、宫格、无文字图、连续性和不可改写因果等必要约束（第 122-135 行）。
- **职责/输入/唯一输出/阻断：通过。** 一个已批准剧本包输入，输出一个结构化 shot package；外部结果限定为 `SUCCESS`、`INPUT_ERROR` 或 `ADAPTATION_FAIL`，`ASSET_BLOCKED` 被正确限定为包内镜头阻断原因（第 29-35、114-120 行）。
- **证据与市场夸大：通过。** 固定样本与三镜 OTIO 结果被限定在结构/交换/帧率验证范围；明确不得从未运行测试声称媒体质量或新 PASS（第 137-141、156-165 行）。与给定 OTIO 三镜真实 PASS 基线一致。
- **最小修改清单：** 无。

### 5. `gpt-image-subject-assets/SKILL.md`：REVISE

- **已验证成果：基本保留。** 保留只读已批准资产、identity master 与状态版本分离、未知不脑补、实际像素回读、原生横屏 `3840x2160` 基线、生成与 QC 职责边界（第 25-31、43-53、81-103、125-133 行）。
- **平台工程与必要门禁：通过。** 没有建设数据库、DVC、通用 runner 或多供应商统一平台；保留来源、充分性、未知、引用顺序、运行时、实际像素、可见事实和唯一结果等必要门禁（第 105-124 行）。
- **职责/输入/唯一输出/阻断：基本通过。** 输入字段和阻断码较清楚；但职责描述把“受控生成”和“正式接受”都纳入本节点（第 13-17、89-101 行），同时又要求 `human_qc=PENDING` 时不得完成，需把运行时提交/人工验收的边界再写成不可混淆的契约。
- **证据与市场夸大：需修订。** 第 131 行的静态基线“`21/21 comparison PASS`、8 个 eligible、13 个 `ASSET_BLOCKED`”不在本次给定真实证据中，若无对应可回读证据，不能作为已验证 PASS 写入。第 86-87 行的横屏原生 `3840x2160` PASS 与给定证据一致，且第 87、133 行正确声明不能外推到 portrait/square 或全部故事资产，应保留。
- **最小修改清单：**
  1. 为第 131 行的 `21/21` 静态基线补上可回读证据路径和运行范围；若没有证据，改为“静态基线待复核/不作为本轮真实 PASS”。
  2. 明确 `SUCCESS` 必须同时满足实际生成、机器像素 QC 和人工可见事实 QC；`human_qc=PENDING` 只能返回阻断或未验收状态，不能进入 `SUCCESS`。
  3. 明确本节点最多负责调用受控执行边界并汇总结果，凭证、队列、重试和实际媒体状态仍由执行器提供，避免被理解成平台工程。

### 6. `minimax-h3-shot-prompt/SKILL.md`：PASS

- **已验证成果：通过。** 保留按镜编译、资产绑定、四类方言边界、提示词与素材绑定分离、执行/QC 职责分开，并明确真实视频未运行（第 13-31、53-81、102-114、125-133 行）。
- **平台工程与必要门禁：通过。** 没有建设通用 runner、数据库、统一供应商接口或平台；保留资产 ID/版本/hash/角色/顺序、方言版本、`execution_status=NOT_RUN` 和媒体验收禁止等必要门禁（第 33-51、115-124 行）。
- **职责/输入/唯一输出/阻断：通过。** 单个闭合版本化 shot package 输入；输出为一个 `SUCCESS` 编译包或一个显式阻断，且不把提交成功写成视频成功（第 15-18、33-42、102-114 行）。
- **证据与市场夸大：通过。** 四种后端仅作为编译方言边界，没有声称可用性、价格、模型、分辨率或运行结果；固定基线明确为静态结构 PASS，执行、绑定、媒体 QC 和视频均未运行/待定（第 53-81、125-133 行）。
- **最小修改清单：** 无。另有格式性提醒：第 3 行使用带引号的 YAML 键名 `"description"`，语义上仍是 description，但合入前宜统一为普通 `description:`，以避免工具链或人工检查误判；这不改变本次验收结论。

## 总结结论

6 份候选稿中，5 份可按当前证据边界判定 `PASS`：写作入口、小说改剧本、剧本资产提取、15 秒分镜、MiniMax H3 逐镜提示词。1 份判定 `REVISE`：GPT Image 主体资产，原因是静态 `21/21` PASS 缺乏本次给定证据支撑，并且生成/人工 QC 的最终 `SUCCESS` 边界需要收紧。未发现其他候选稿把 H3 未运行视频写成真实视频 PASS，也未发现把平台工程作为本轮交付必需部分保留下来。

## 回读核验记录

报告写入后已回读核验：6 个候选稿结论、四项验收记录、REVISE 最小修改清单均存在且内容可读；本报告回读总行数为 **86 行**。本次未修改任何候选稿或原 Skill。
