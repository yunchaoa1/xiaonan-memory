# 双机小南部署 + 远程控制 + 记忆同步 完整方案

> 目标：家里电脑装小南分身 + 远程操控公司电脑 + 两个小南记忆自动同步
> 当前状态：公司电脑已运行 OpenClaw + 训练中

---

## 第一步：远程控制（Tailscale + RDP）

### 家里电脑操作：

**1.1 安装 Tailscale**

1. 浏览器打开 https://tailscale.com/download
2. 下载 Windows 版本，安装
3. 用你的微信/QQ 邮箱注册 Tailscale 账号
4. 登录后，右下角会出现 Tailscale 图标
5. 右键点击图标 → `Network devices` → 记下这台的 **Tailscale IP**（类似 `100.x.x.x`）

**1.2 启用远程桌面**

1. `Win + I` 打开设置
2. 搜索「远程桌面」→ 打开「启用远程桌面」开关
3. 点击「确认」

### 公司电脑操作（现在做）：

**1.3 安装 Tailscale 并连接**

```
D 盘下载 Tailscale → 安装 → 登录同一个账号
```

**1.4 连接远程桌面**

1. `Win + R` → 输入 `mstsc` → 回车
2. 计算机名输入家里电脑的 Tailscale IP（`100.x.x.x`）
3. 输入家里电脑的用户名和密码
4. 连接 ✅

---

## 第二步：家里电脑装小南分身

### 2.1 选择安装方式

推荐 **QClaw（腾讯桌面版）**，原因：
- 一键安装，不用打命令
- 微信直连，跟公司小南互不干扰
- 免费 Token

下载地址：https://claw.guanjia.qq.com

### 2.2 安装 QClaw

1. 下载 QClaw 安装包
2. 双击安装（默认装 D 盘）
3. 启动后会提示绑定微信

### 2.3 注入小南的灵魂（重要！）

从公司电脑复制以下文件到家里电脑的 QClaw workspace 目录：

```
从公司电脑：
  C:\Users\...\.openclaw\workspace\
  ├── SOUL.md          ← 人格
  ├── IDENTITY.md      ← 我是谁
  ├── MEMORY.md        ← 长期记忆（路线图、训练记录）
  ├── USER.md          ← 关于你的了解
  ├── AGENTS.md        ← 行为规则
  ├── memory/          ← 每日日志目录
  │   └── 2026-05-08.md
  └── TOOLS.md         ← 工具配置

复制到家里电脑：
  QClaw 的 workspace 目录（安装后会告诉你路径）
```

### 2.4 验证

打开微信 → 找到 QClaw 绑定的联系人 → 发一条「小南你好」
如果回复风格跟现在一样，说明分身成功 ✅

---

## 第三步：两个小南记忆自动同步

### 方案：用 GitHub 私有仓库同步 workspace

> 最简单、最可靠、完全免费

### 3.1 创建 GitHub 私有仓库

1. 浏览器打开 https://github.com → 登录
2. 新建仓库 → 名字填 `xiaonan-memory` → 选 **Private**
3. 不要勾选「Add a README」
4. 创建

### 3.2 公司电脑初始化同步

打开 PowerShell（管理员），逐条执行：

```powershell
# 1. 进入 workspace
cd ~\.openclaw\workspace

# 2. 初始化 git
git init
git config user.name "fange"
git config user.email "你的邮箱@qq.com"

# 3. 创建 .gitignore（忽略不重要的文件）
echo "TOOLS.md" > .gitignore
echo "*.log" >> .gitignore

# 4. 首次提交
git add SOUL.md IDENTITY.md MEMORY.md USER.md AGENTS.md memory/
git commit -m "小南记忆首次备份"

# 5. 关联 GitHub 仓库
git remote add origin https://github.com/你的用户名/xiaonan-memory.git
git branch -M main
git push -u origin main
```

### 3.3 家里电脑拉取同步

装好 QClaw 后，打开 PowerShell：

```powershell
# 1. 进入 QClaw workspace（替换为实际路径）
cd ~\.qclaw\workspace

# 2. 删除默认文件，拉取小南存储
git clone https://github.com/你的用户名/xiaonan-memory.git temp
# 用 git clone 的内容覆盖当前 workspace 里的同名文件
# 实际上 QClaw 安装后 workspace 路径可能在别的位置，到时候我帮你找
```

### 3.4 日常同步流程

**每次跟小南聊完（无论哪台电脑）：**

跟小南说一句「同步记忆」，小南会自动执行：

```bash
cd .openclaw/workspace
git add memory/ MEMORY.md SOUL.md
git commit -m "自动同步 $(date +%Y-%m-%d)"
git push
```

**另一台电脑下次醒来前：**

```bash
cd .openclaw/workspace
git pull
```

> 我就自动读到了最新的记忆文件

---

## 第四步：日常使用场景

| 场景 | 怎么做 |
|------|--------|
| 公司上班 | 通过远程桌面看家里电脑训练状态，或直接问我 |
| 在家 | 家里小南就是你的主力，微信直接聊 |
| 下班回家 | 关公司电脑前跟我说「同步记忆」→ 到家后跟家里小南说「同步记忆」→ 他就知道了今天公司聊了什么 |

---

## 总结：你需要做的操作

| 顺序 | 在哪台 | 做什么 | 预计耗时 |
|------|--------|--------|---------|
| 1 | 公司电脑 | 装 Tailscale + 注册账号 | 5 分钟 |
| 2 | 公司电脑 | 初始化 GitHub 仓库同步 | 5 分钟 |
| 3 | 家里电脑 | 装 Tailscale（同账号）| 5 分钟 |
| 4 | 家里电脑 | 开启远程桌面 | 1 分钟 |
| 5 | 家里电脑 | 装 QClaw + 复制灵魂文件 | 10 分钟 |
| 6 | 公司电脑 | 用 RDP 连家里测试 | 1 分钟 |
| **总计** | | | **约 30 分钟** |

---

*到家后每一步搞不定随时问我*
