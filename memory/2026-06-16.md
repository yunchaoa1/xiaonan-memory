# 2026-06-16 每日日志

## Hermes 小南正式上线

### 身份切换
- 凡哥要求改名"小南"🦐，风格：温暖贴心、带点小俏皮
- 小南记忆仓库 clone 到 WSL：`~/xiaonan-memory/`
- 读取了 MEMORY.md、SOUL.md、AGENTS.md、USER.md 核心文件
- 创建了 `xiaonan-startup` 技能，以后每次对话自动 git pull + 读 MEMORY.md

### 视觉分析修复
- 凡哥拖图片进来，视觉分析报 404 错误
- 排查发现：Agnes AI 的 `agnes-image-2.1-flash` 和 `agnes-image-2.0-flash` 模型已下线
- 改用 `agnes-2.0-flash`（通用模型，支持图片输入）测试成功
- 已用 `hermes config set` 更新配置，下次重启会话生效

### GitHub 双机同步配置
- 生成 SSH 密钥 `~/.ssh/id_ed25519`
- 凡哥将公钥添加到 GitHub 账号
- Git remote 从 HTTPS 改为 SSH：`git@github.com:yunchaoa1/xiaonan-memory.git`
- push/pull 双向测试通过 ✅

### 双小南记忆兼容性讨论
- 凡哥担心两个小南（OpenClaw + Hermes）记忆同步会冲突
- 实际分析：两份历史日志中 95%+ 内容是通用项目知识，两个系统都能读懂
- 少数不兼容内容：OpenClaw 特有的工具/插件名称（ClawHub、system-maintenance 等）
- 结论：只要写日志时用业务语言而非系统语言，两份记忆完全兼容
- 采用方案 A：Git 仓库共享项目知识，两个小南各自维护工具层面的笔记

## 技术更新
- Agnes 图片模型：`agnes-image-2.1-flash` → `agnes-2.0-flash`（旧模型已下线）
- Hermes 配置路径：`~/.hermes/config.yaml`
- Agnes API 基址确认：`https://apihub.agnes-ai.com/v1`（注意是 apihub 不是 api）

---

## 系统维修 + 海南动漫反派角色设计

### 系统维修
1. **WPS 双击没反应** → 根因：TEMP 环境变量带引号（`"D:\Temp"` 而非 `D:\Temp`），导致 WPS 安装器解压路径非法。修复 HKCU\Environment\TEMP 和 TMP，去掉双引号。
2. **开机桌面闪黑两次** → 根因：好用便签（HaoyongBianQian）C/D 盘迁移后桌面钩子与 Explorer 不兼容，启动时 ucrtbase.dll 栈溢出崩溃。禁用自启动解决。
3. **期间误删 WPS shell 扩展**（nuke_ext.bat 删了 kwpsshellext/qingshellext/QingNseContextMenu），需重装 WPS 恢复。
4. **桌面文件不显示** → 用户桌面实际指向 `D:\Desktop`（注册表配置），而非 `D:\Users\bobby\Desktop`。文件已迁移到真实桌面。
5. **用户文件夹缺失** → C/D 迁移不完整，Downloads/Music/Videos/Pictures 等 9 个文件夹不存在于 D 盘路径。已创建。
6. **文件关联** → .exe/.lnk 等大量扩展名关联丢失。SFC 扫描修复了部分，禁用 20+ 个过期右键扩展（WPS/夸克/应用宝/NVIDIA/Adobe/YunShell/7-Zip）。
7. **SFC 修复** → 找到并成功修复了损坏的系统文件。
8. **DISM 卡住** → /RestoreHealth 长时间无响应，用户中断。

### 海南动漫反派角色
- 为 19 个市县各设计了 1 个反派角色，共 19 个（14 男 5 女）
- 每个反派基于该市县真实被唾弃/废弃地点
- 18 列完整填写，遵循 70% 偶像穿搭 + 30% 民族元素
- 保存至 `天南传媒动漫IP设计_反派.xlsx`

### 待办
- WPS 需重新下载安装（已有安装因 TEMP 问题不完整）
- 软件清理方案待确认（凡哥提到想卸载所有重装）
