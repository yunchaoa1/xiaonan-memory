# 2026-05-09 日志

## 白绮罗穿书 LoRA 训练

### 第 2 次训练崩溃（10:33-11:37）
- 凡哥上午重启训练，配置改为：
  - 目标 3000 步（原 5000），save_every=500（原 250），分辨率固定 1024
  - 速度约 3.1s/it，预计 ~2.6h 完成
- 11:28 左右崩溃于 2707/3000（90%），进程无声死亡（PID 33620 僵尸）
- GPU 0%，但显存残留 15.6GB 未释放
- 日志无报错，最后一条在 step 2707
- 已有 checkpoint：1000、1500、2000、2500（每种 85MB）

### 关键发现：AI Toolkit 原生断点续训
- 查 BaseSDTrainProcess.py 源码确认：
  - 第 1991-2016 行：自动从 optimizer.pt 加载优化器状态
  - 第 858 行：从 checkpoint 元数据读取步数 → set start_step
- **不需要任何配置**，直接重跑 `python run.py output/xxx/config.yaml` 即可续训
- 已写入 MEMORY.md 长期记忆

### 第 3 次训练（11:48 开始，续训）
- 杀僵尸进程（PID 33620）→ 释放显存
- 备份 checkpoint + optimizer.pt + config.yaml 到 `_backup` 目录
- 直接重跑原 config → 自动从 2500 步续训
- GPU 99%，75°C，预计 ~26 分钟完成（剩余 ~500 步）
- ⚠️ 第一次启动因 exec timeout 设置太短被 SIGTERM，第二次用 no-timeout background 成功

### GPU 训练异常监控机制
- Cron job `90c965a2` 每 5 分钟巡检
- `memory/training_monitor.json` 记录状态

### 今日沟通
- 凡哥问 GPU 四个引擎（3D/Copy/Video Encode/Video Decode）含义 → 解释了 LoRA 只用 3D，其他 0% 正常
- 凡哥要求把断点续训知识写入长期记忆 ✅

## 白绮罗 LoRA 测试（12:32-13:02）
- ComfyUI WSL 启动在 8189 端口
- 使用 `z-image文生图（lora版）.json` 工作流
- 测试参数：25步、CFG 4.5、1024×1024、seed=42、euler/simple
- 18 张图（6 列×3 行）全部成功
- 生成速度：~45s/张（Z-Image 25步）
- 结果已发飞书三行对比图

### 测试脚本位置
- `lora_test.py` 在 workspace，可复用
- 18 张原图：`/tmp/bql_test/`
- 对比图：`/tmp/bql_row_1/2/3.png`
- API 结果：`lora_test_results.json`

### 文件名映射规则
- bql_xxxxx_00001_.png = 正面站立全身照
- bql_xxxxx_00002_.png = 侧身回眸
- bql_xxxxx_00003_.png = 坐姿托腮

## 自动续训完成（17:34 cron 触发）

### 训练状态
- auto_resume_training.py 检测到训练已停，步数=5000
- 状态：**completed**（已到达 5000 步，完成全部 10 段分段训练）
- 训练名称：白绮罗穿书_lora_5000，rank=32

### LoRA 测试
- 启动 ComfyUI WSL（8189 端口 + --lowvram）
- 5000步 checkpoint 的 3 张图 → 全部成功 ✅
- 500步 checkpoint 的 3 张图 → 全部成功 ✅  
- 无LoRA 基线的 3 张图 → 全部成功 ✅
- 中间 checkpoint（1000~4500）未保留（save_every=500, max_step_saves_to_keep 限制）
- 3×3 对比网格已生成：bql_comparison_grid.png

### 飞书通知
- 已发送对比网格图 + 训练摘要给凡哥
- 提醒中间 checkpoint 丢失问题

### 注意事项
- 训练实际跑到 5000 步（而非原计划 3000），自动续训系统一直续到训练自然停止
- 中间段可能因 max_step_saves_to_keep 限制被覆盖

## 关机前状态（18:43）

### 训练进展
- 第1段 0→500 ✅
- 第2段 500→1000 ✅
- 第3段 1000→1500 ✅
- 第4段 1500→2000 ✅
- 第5段 2000→2500 ❌ 异常中断（速度崩到91s/it）
- 第6段 2500→3000 ⏳ 待续

### 当前产物
- checkpoint: 500步（备份）+ 2000步（最新）
- optimizer.pt 完好，可断点续训

### 回家后操作
1. 杀僵尸进程
2. config steps=2500（需重跑第5段）
3. 启动训练 → 第6段 steps=3000 → 完成

## 20:19 自动续训 — 第6段启动
- 脚本路径 bug：反斜杠未转正斜杠 → 已修复（新增 _win2wsl() 函数）
- 第5段 2000→2500 已完成，checkpoint 2500 步已备份
- 第6段（最后一段）2500→3000 已自动启动
- 速度 ~3.97s/it，预计 ~33min 完成（20:52 左右）
- 飞书通知已发 ✅

## 20:24 训练全部完成 🎉
- cron 自动巡检检测到训练已停，步数=3000（target=3000）→ completed
- 全部 6 段完成：0→500→1000→1500→2000→2500→3000
- 最后一段速度 ~0.60s/it，平滑后 ~2.49s/it
- 飞书通知已发，等待凡哥说「开始测试」
- training_monitor.json → active=false, status=completed

### 今日关键产出
- AI Toolkit 断点续训机制验证 ✅
- 分段训练策略确立（500步/段）✅
- 动态速度检测脚本完成 ✅
- 自动续训 + cron 部署 ✅
- Checkpoint 备份机制 ✅
- LoRA 测试标准流程写入 MEMORY.md ✅
- 训练 SOP 完整写入 MEMORY.md ✅
