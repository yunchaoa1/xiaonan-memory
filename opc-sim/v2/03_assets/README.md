# OPC v2 资产提取包

这是小说改剧本之后的唯一资产提取结果。

- `asset_package.json`：机器可读的唯一 `SUCCESS` 资产包
- `asset_manifest.md`：统计、边界和场景索引

本节点继承上游锁定画风 `3d_animated_film`，不重新选择画风。资产提取只负责身份、状态、场景、连续性道具、声音和场景绑定，不负责镜头设计、提示词、渲染或视频生成。

普通厨房道具只作为场景动作媒介；独立主体资产仅保留两名人物、连续住宅空间、周野的包和钥匙。