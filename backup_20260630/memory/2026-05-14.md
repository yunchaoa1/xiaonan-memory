# 2026-05-14 日志

## 齐镇海数据集生成分析（08:48-09:51）

### 问题发现
- 凌晨电脑重启，数据集只到 241/290 张（缺口 49 张）
- 凡哥还发现两次"生成20多张后被清空重来"

### 根因分析
1. **出图慢根因**：77% 时间浪费在换 prompt 间隙（12-15分钟/批），单张实际 37 秒
2. **清空重来根因**：脚本开头 `shutil.rmtree(OUTPUT)` 每次重启删掉所有产出
3. **API 不稳定**：无重试机制 + 轮询竞争条件
4. **影子乱根因**：提示词"纯白背景"+"清晰的浅灰色阴影"是物理矛盾，Qwen Image 模型对光影关系理解弱

### 修复行动
1. ✅ v4 脚本：删除 shutil.rmtree + 续传逻辑 + 顺序跑完每个 lid
2. ✅ 三个脚本全部删除影子描述（24 处）
3. ✅ 文档 `3训练人物lora.docx` 删除影子描述 + 添加错误案例配图（4张）

### LoRA 全自动流水线框架确立
- 凡哥要求：以后做任何角色都按 6 步流水线
- 已写入 MEMORY.md 永久遵守
- 当前进度：3/6 步已攻破，3 步待开发
- 逐步击破，不赶工

### ComfyUI 启动踩坑
- WSL 直调 Windows Python 报 Permission denied
- 需用 cmd.exe 传 Windows 路径启动
- 启动命令：`cmd.exe /c "D:\SDkecheng\ComfyUI\.venv\Scripts\python.exe D:\SDkecheng\ComfyUI\main.py --listen 0.0.0.0 --port 18188"`

## .done 标记机制（11:21）

### 问题
凡哥手动筛选图片时删除不满意的 → v4 续传检测到"不满 10 张"就自动补全 → 冲突

### 修复：v4.2
- 每个 lid 首次生成满 10 张后，自动创建 `.done_XXX` 标记文件
- 续传时：有 `.done` 标记 → 跳过该 lid（不管现有多少张）
- 如需重新生成，删除对应 `.done` 即可

### v4 版本演进
- v4.0: 续传 + 不去重
- v4.1: + WSL 文件系统安全检查
- v4.2: + .done 标记机制

## 齐镇海 LoRA 训练完成（23:03）

### 结果
- 训练完成：3000/3000 步
- 最终 LoRA：`齐镇海_lora.safetensors` (85MB)
- Checkpoints：1000, 1500, 2000, 2500, final
- 数据集：25 张精选（4 个 lid 废弃：16/17/18/28）
- 触发词：天南齐镇海
- 参数：rank 16, lr 1e-4, AdamW8Bit, EMA, 反蒸馏适配器 v2

### 训练踩坑
- AI Toolkit .venv 路径是 venv 不是 .venv
- config 需放在 config/ 下（不是 output/）
- YAML 中 Windows 路径不能用反斜杠，需用正斜杠
- 僵尸 Python 进程占 GPU 导致训练无法启动，taskkill 清掉后恢复
- exec timeout 杀掉过第一次训练（304/3000），第二次成功

## LoRA 测试（23:57-00:20）
- 标准矩阵：5 checkpoint × 3 姿势 = 15 张
- 强度对比：2500/3000 × 4 强度 = 8 张
- 测试结果已发飞书
- 凡哥关机

## 今日总结
- 数据集生成从 v3 → v4.2（.done 标记、续传、安全检查）
- 禁止清单 10 条写入长期记忆
- 26→25 张精选训练集
- 训练完成：齐镇海_lora 3000 步
- LoRA 测试完成，待凡哥确认效果
