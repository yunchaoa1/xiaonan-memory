
## 2026-05-07 LoRA 训练环境搭建（重要）

### 硬件限制
- 凡哥的笔记本：RTX 5080 Laptop 16GB VRAM + 16GB RAM
- WSL2 内 Z-Image 训练无法完成（16GB RAM 不够，swap 颠簸导致死锁）

### 环境规则（永久遵守）
- **凡哥的一切 Windows 操作必须在 D 盘**，绝不动 C 盘
- **Windows 上 ComfyUI 有自己独立的 Python，绝对不能碰**
- AI Toolkit 如果装 Windows 上，路径是 `D:\ai-toolkit-win\`，与 ComfyUI 彻底隔离
- 规则文件：`skills/comfyui-wsl/windows-aitk-rules.md`

### AI Toolkit 训练失败的已知 Bug
- `cache_latents_to_disk: true` → 死锁
- `adamw8bit` 优化器 → C 库加载失败
- `num_workers > 0` → DataLoader 子进程死锁  
- `low_vram: true` → swap 颠簸
- 修复：`low_vram: false`, `adamw`, `num_workers: 0`, `cache_latents: false`

### 已下载资源
- Z-Image-Turbo 完整模型：`D:\SDkecheng\models\z-image-turbo\`（31GB）
- Qwen3 文本编码器（已合并）：`D:\SDkecheng\models\text_encoders\z_image_qwen3_4b.safetensors`（8GB）
- Z-Image VAE（已提取）：`D:\SDkecheng\models\vae\z_image_vae.safetensors`
- 旧数据集（废弃）：`D:\SDkecheng\天南白绮罗qwen 2511 lora训练数据集\`（29张，效果差）
- 新数据集：`D:\SDkecheng\白绮罗穿书_processed\`（35张，avg=1024，标注完成）

## 2026-05-07 白绮罗穿书 LoRA 数据集重建

### 废弃原因
旧数据集 2000 步 LoRA 画质崩塌，仅学到红色领口和白色头花的雏形，服装细节基本丢失。

### 角色设计（白绮罗）
- **服装**：朱红色改良汉服劲装，交领右衽，白色内衬领口，红色暗纹提花面料，黑色宽腰带+金属扣环+飘带，红裙+内层红色灯笼裤
- **发型**：半扎半披，上半用发簪+红色绑带扎发髻，下半黑色长发垂至腰间，空气刘海
- **饰品**：银色几何星形耳坠
- **护具**：左前臂黑色护腕+红色交叉绑带
- **鞋履**：黑色高筒皮靴
- **风格**：武侠古风，干练利落

### LoRA 标注正确原则（关键学习）
- **标注里写的 = 不希望 LoRA 学习的（可变特征）**：姿态、表情、光线、背景、视角
- **标注里不写的 = 绑定到触发词「白绮罗」的（角色特征）**：服装、发型、饰品、五官
- 已按此原则完成 35 条标注

### 待训练
- 目标步数：5000
- 底模：Z-Image-Turbo-FP8
- checkpoint 每250步保存（最多保留4个）
- 训练已于 2026-05-07 21:43 启动（AI Toolkit CLI），GPU 100% 满载运行中（确认于 2026-05-08 09:03）
- **注意**：训练速度极慢（Z-Image 是大模型），12 小时后仍未出第 1 个 checkpoint（250 步），5000 步预计需数天至数周
- **重要发现**：`max_step_saves_to_keep: 4` 是滚动保留（250 步开始保存，不是攒够 4 个），目录中无 checkpoint 说明还没到 250 步
- **凡哥决定**：不关机，电脑留在家继续跑训练
- **⚠️ 2026-05-08 19:37 蓝屏崩溃**：训练运行 22 小时后 GPU 驱动崩溃（错误码 0x00020001），进程死亡，无 checkpoint 产出
  - 驱动：NVIDIA 591.91（CUDA 13.1）— RTX 5080 Laptop
  - 两次相同崩溃（另一次：2026-04-28），确认是 GPU 驱动长时间满载不稳定
  - 训练需从头重新启动
- 旧 LoRA（1000/1250/1500/1750/2000）已全部删除

### 训练准备流程（2026-05-07）
1. **调整分辨率**：将原始数据集图片统一调整为 1024×1024（avg=1024），确保输入尺寸一致
2. **数据标注**：按「标注里写的=可变特征，不写的=角色特征」原则，完成 35 条图文标注
3. **训练配置**：
   - 底模：Z-Image-Turbo-FP8
   - 优化器：adamw（非 adamw8bit）
   - num_workers: 0（避免 DataLoader 死锁）
   - low_vram: false
   - cache_latents_to_disk: false
   - 目标步数：5000
   - checkpoint：每250步保存，最多保留4个
4. **训练方法**：在 Windows 侧通过 AI Toolkit CLI 启动（`D:\ai-toolkit-win\`），WSL2 仅用于辅助操作

## 记忆管理策略（重要 - 2026-05-08 建立）

### 策略
- **每日日志（memory/YYYY-MM-DD.md）**：记越详细越好，半年滚动清理（不重要内容新替旧）
- **MEMORY.md**：长期重要记忆，永久保留
- **自动沉淀**：小南自行判断记录时机，不等凡哥开口
- **触发条件**：技术决策、参数变更、凡哥偏好/习惯、讨论结论、任何凡哥下次不想重复解释的事
- **关机前**：凡哥说「要关机了」→ 回顾当日内容写日志
- **半年清理**：不重要记忆半年后清理，重要的一直保留

### 恢复机制
- 会话崩溃后重启，小南自动读取当日 memory/YYYY-MM-DD.md + MEMORY.md
- 凡哥说「看看今天的日志，咱们继续聊」即可快速恢复

### 沟通规则
凡哥要求每次商讨结果、结论都做总结写入 MEMORY.md

### 开机启动已设置
- 企业微信 + 微信 → Windows 启动文件夹（`skills/comfyui-wsl/windows-aitk-rules.md` 同级）

## 2026-05-07 沟通规则强化（重要）

凡哥要求增加一条规则：
- **即使他说「开始干」「动手吧」等直接指令，小南也必须先列出行动计划再反问确认**，不能直接执行
- 目的：帮凡哥检查遗漏，同时也是帮他梳理任务流程
- 已写入 SOUL.md Boundaries 部分

## 2026-05-08 开源短剧管线计划（重要）

### 7 步管线蓝图
```
1.剧本(LLM) → 2.角色(ComfyUI+LoRA) → 3.分镜(LLM) → 4.关键帧(ComfyUI) → 5.视频(LTX-2.3) → 6.配音(TTS) → 7.合成(ffmpeg)
```

### 执行策略
- **逐个击破**：等白绮罗 LoRA 跑完，从步骤 1 开始
- **凡哥角色**：创意总监 + 审核（每步产出后说「过」或「改」）
- **小南角色**：全自动执行引擎（写脚本、调参数、串联流程、监控状态）
- **目标**：凡哥只做审核，小南负责 1→7 全流程执行

### 各步骤状态
| 步骤 | 状态 | 待办 |
|------|------|------|
| 1 剧本 | 待开始 | 定制 prompt 模板 |
| 2 角色 | 进行中 | 白绮罗 LoRA 训练 |
| 3 分镜 | 待开始 | 剧本→分镜自动化模板 |
| 4 关键帧 | 部分可行 | ComfyUI API 批量调用 |
| 5 视频 | 待探索 | LTX-2.3 本地部署 |
| 6 配音 | 待探索 | GPT-SoVITS 训练 |
| 7 合成 | 脚本化 | ffmpeg 自动化 |

### LTX-2.3 关键信息
- 以色列 Lightricks 开源，22B 参数 DiT 架构
- 音画同出（端到端音视频联合生成）
- 原生 ComfyUI 支持，支持 LoRA 微调
- 最高 4K/20 秒/48FPS，原生 9:16 竖屏
- 有蒸馏加速版（适配凡哥硬件）

## 自动清理规则

**磁盘空间管理**：任何下载的文件如果出现以下情况，立即删除：
- 下载中断/失败留下的残留文件（.incomplete、.lock 等）
- 旧版本被新版本替代
- 下载了但不能用的文件
- 临时文件用完后

同时写入 SOUL.md 作为自动触发行为。
